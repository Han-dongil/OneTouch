<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onetouch.web.mtr.inForm.dao.InMapper">
	<select id="list" resultType="com.onetouch.web.mtr.inForm.dao.InVO">
		SELECT * FROM MTR_IN
	</select>
	<select id="select" resultType="com.onetouch.web.mtr.inForm.dao.InVO">
		SELECT A.IN_NO, TO_CHAR(A.IN_DATE, 'yyyy-MM-dd') AS IN_DATE, A.MTR_CD, B.MTR_NM, B.UNIT, A.ORD_NO, 
		A.FLT_AMT, A.IN_AMT, A.UNIT_COST, A.TOT_COST,
		FN_GET_BAS_DTL_CD_TO_NM(B.COMP_CD) AS COM_NM  
		FROM MTR_IN A, ADM_MTR B
		WHERE A.MTR_CD = B.MTR_CD
		
		<if test="startDate != null and startDate != ''"> 
			<![CDATA[AND TO_CHAR(A.IN_DATE, 'yyyy-MM-dd') >= #{startDate}]]>
		</if>
		<if test="endDate != null and endDate != ''"> 
			<![CDATA[AND TO_CHAR(A.IN_DATE, 'yyyy-MM-dd') <= #{endDate}]]>
		</if>
		<if test="inComCd != null and inComCd != ''">
			AND B.COMP_CD = #{inComCd}
		</if>
		<if test="ditemCode != null and ditemCode != ''">
			AND B.MTR_CD = #{ditemCode}
		</if>
	</select>
		<!-- SELECT * 
		FROM MTR_IN A LEFT JOIN ADM_MTR B 
		ON A.MTR_CD = B.MTR_CD -->
	<insert id="insert">
		INSERT INTO MTR_IN 
		(IN_NO,
		MTR_CD,
		IN_DATE,
		ORD_NO,
		UNIT,
		IN_AMT,
		FLT_AMT,
		UNIT_COST,
		TOT_COST
		)
		VALUES(
		FN_MAKE_CODE_COL2('MIN', 'MTR_IN', 'IN_NO', 'IN_DATE'),
		#{mtrCd},
		SYSDATE,
		#{ordNo},
		#{unit},
		#{inAmt},
		#{fltAmt},
		#{unitCost},
		(#{unitCost}*#{inAmt})
		)
	</insert>
	<update id="update">
		UPDATE MTR_IN
		SET
		MTR_CD = #{mtrCd},
		UNIT = #{unit},
		IN_AMT = #{inAmt},
		FLT_AMT = #{fltAmt},
		UNIT_COST = #{unitCost},
		TOT_COST = (#{unitCost}*#{inAmt})
		WHERE
		IN_NO = #{inNo}
	</update>
	<!-- 하나씩 삭제 -->
	<delete id="delete">
		DELETE 
		FROM MTR_IN
		WHERE IN_NO = #{inNo}
	</delete>
	
	<!-- 리스트로받아서 for문으로 다 삭제하는쿼리 -->
	<!-- <delete id="delete">
		DELETE 
		FROM MTR_IN
		WHERE IN_NO IN
		<foreach collection="list" item="vo" open="(" close=")" separator=",">
		#{vo.inNo}
	</foreach>
	</delete> -->
</mapper>